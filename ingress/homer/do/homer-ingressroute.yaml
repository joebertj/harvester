apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homer
  namespace: homelab
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`{{ base_domain }}`)
      kind: Rule
      services:
        - name: homer
          port: 80
  tls:
    secretName: homer-tls
