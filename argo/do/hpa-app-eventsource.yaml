apiVersion: argoproj.io/v1alpha1
kind: EventSource
metadata:
  name: hpa-app-webhook
  namespace: argo-events
spec:
  service:
    ports:
      - port: 12000
        targetPort: 12000
  http:
    push:
      port: "12000"
      endpoint: /push
      method: POST
